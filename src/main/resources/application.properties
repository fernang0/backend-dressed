spring.application.name=backend
spring.docker.compose.enabled=false

app.jwt.secret=${APP_JWT_SECRET:change-this-secret-with-at-least-32-characters}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:3600000}